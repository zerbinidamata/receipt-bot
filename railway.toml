# Railway configuration for Recipe Bot (Go service)
# This is the main Telegram bot service (polling, no HTTP server)

[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "always"
numReplicas = 1
