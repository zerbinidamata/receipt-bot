# Railway configuration for Python Scraper Service
# This is the gRPC service for video scraping and transcription
# Set Root Directory to "python-service" in Railway dashboard

[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckTimeout = 300
restartPolicyType = "always"
numReplicas = 1
